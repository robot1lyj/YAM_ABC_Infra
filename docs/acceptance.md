# 验收状态与证据入口

最近核对：2026-09-24。这里区分现场运行状态与带日期的历史测试；旧失败和测量归 [历史验收快照](archive/acceptance_20260922.md) 及原始证据，不把旧“最新一集”当成现在。

## 当前现场状态与历史基线

2026-09-24 只读查询：IPC 仓库 HEAD 为 `76b6834`，设备和 Web 服务 active；四臂与相机均 disconnected，设备无报错。Web 已单独安装按当前 `wlan0` 地址绑定的启动脚本，面板最近实测 HTTP 200。本地源码、IPC checkout 与运行服务不应当作同一版本；后续部署前逐项核对。

2026-09-22 在提交 `0bc1be5` 上的离线恢复加固测试（529 passed、4 skipped、17 subtests）仅说明当时源码的测试范围，详见[原始证据](evidence/20260922-architecture-recovery.json)。该结果不说明2026-09-24的现场版本已经完整部署，也不能替代真机实时性、抓取或长时录制验收。

## 已实现与部署差异

| 能力 | 当前代码 | 现场状态 |
|---|---|---|
| 四模式、绝对遥操作、HIL手柄锁定/页面交还 | 已实现 | 有历史现场使用；本轮不复测 |
| 同步50步 / TDA / 训练式RTC | 三种独立时间合同 | 有历史联调与录制，不代表当前模型效果 |
| Web / Thor / 规划 / 录制子进程分离 | 已实现 | 基础分进程有现场记录；具体恢复改动按 IPC 实际 HEAD 与服务版本逐项核对 |
| 独立SDK executor | 可选、离线Unix生命周期测试 | 尚未迁移IPC，默认device仍拥有SDK |
| 系统盘运行时 + NVMe数据 | 配置模板和挂载预检 | 尚未现场迁移 |
| 故障原始集恢复 | segment/spool可读部分恢复到新目录 | 不保证恢复未落盘RAM、坏盘所有帧 |
| HIL两类等待过滤 | 录制侧实现，每集独立摘要、连续训练时间 | 应核对新采实际字段，旧集不自动改写 |

最新现场进度归 [续作检查点](cache/checkpoint.md)。历史重启许可不构成新的运动或设备进程重启许可。

## 现场验收清单

本轮语义核对另外发现两个尚未改动的代码残留：逐帧`policy_url`仍记录来源地址；`interaction_rules.py`辅助对齐超时提示仍写“相对遥操作”，实际`manual_ready`已调用共用绝对映射。这些需后续清理，不应因文档修正就宣称代码提示也已修复。

一次获准停测窗口集中部署设备核心，避免连续重启。更新前检查实际服务启动参数、挂载、保存是否完成、SDK所有权和四臂支撑。

1. 无运动：Web独立重启、策略/规划重载；设备连接与HOLD不丢失、不自动恢复运动。
2. 局部故障：录制worker恢复新session、相机失败重试、策略错误与硬件FAULT分开；每步页面给出中文原因及可用恢复操作。
3. 用户操作：暂停后切任务、不混写；HIL介入→右①人工→①锁定→页面交还；相同人工实现覆盖纯遥操作/采集/HIL。
4. 数据：三路视频帧数对齐行数、14D字段/有效标记、两类等待删除、原始tick/time保留、frame_index/timestamp连续、摘要只属当前集。
5. 性能：不录制与三相机录制运动对照、长时录制/停录排空、CPU/内存/队列/磁盘与控制p99。没有长时证据不声明任意录制时长。
6. 恢复安全：恢复成功仍HOLD，旧epoch/旧计划不能执行；SDK异常不能通过清页面提示或重载模型绕过。

真实拔盘/USB异常可能释放力矩或损坏数据，不在本地故障注入测试范围内，不未经现场安排复现。

## 历史证据怎么用

历史日志覆盖早期相对映射、ensemble/raw、100Hz二阶/纯线性试验、旧限速、RTC探针以及HIL录制。只有带日期与范围的结果可引用；当前合同以 [架构](dagger_architecture.md)、[采集](collect.md)、[模型接口](condapi_interface.md) 为准。

报告必须写清：代码/配置版本、真实或mock、输入是否真实相机、是否运动、样本量/时间窗口、原始证据位置、失败与未验证边界。不得把指令速度当反馈速度，不能用一次成功推断故障根因。
