# 验收证据与下一步

## 软件基线

运行代码基线：`1c04c83`。[历史证据](evidence/20260908-hil-runtime-v1.json)记录：

- 全套161通过、2跳过、9个subtest通过；最终HIL相关22项通过。
- 4秒模拟120个tick完成策略→人工→恢复，三路视频与专家导出可读取。
- 本机HTTP检查三模式、接管、保持、成功标记和退出，输出63条记录且无错误。
- 12秒模拟保持360个tick，未计到deadline miss。

这些数字只适用于报告指纹对应的代码和开发环境，不是现在的设备在线状态、RK3588吞吐或实际任务效果。本轮在此基线上新增采集和双按钮仲裁；最新离线验证见下方。

## 本轮四模式、记忆与文档验收

全套 **172 passed、2 skipped、9 subtests passed**。新增测试覆盖手动多段、独立视频索引、空闲不录制、模式边界、队列反压、两个手柄优先级、持续保持与释放、采集导出、文档断链和证据篡改。证据见 [四模式报告](evidence/20260908-collection-buttons-v1.json)。

执行 `python3 scripts/check_project_memory.py` 检查当前入口、文件链接、经验记录和依赖哈希。
报告中的validated_records表示通过指纹/范围结构检查，语义仍需人工/代理审核；不表示记录中的环境目前在线。
文档工具回归覆盖断链、证据改写及历史记录降级审阅，结果由本轮证据记录维护。

## 真机尚未完成

| 阶段 | 输入 / 条件 | 输出证据 |
|---|---|---|
| 实物映射 | [硬件待确认项](workstation.md) | CAN/序列号/夹爪类型对应关系 |
| 逐对低速核验 | 安装固定、人员照看、动作方向确认 | 重力补偿、夹爪、手柄、Leader增益记录 |
| 三路采集 | 三台D405与USB拓扑 | 帧龄/接收偏差/队列/视频完整性 |
| Thor冻结观测 | checkpoint/norm/prompt/action_dt已核对 | 输入输出、shape/单位、延迟和模型身份 |
| 四臂HIL | 前面阶段通过 | 接管/恢复、错误路径、数据回放 |
| 持续任务 | 真实任务样本 | 运行稳定性、资源走势、任务结果 |

先补 [优化评审](optimization_review.md) 的可靠性项，再按上述顺序现场验证。不要把安装环境或模拟成功跳级写成真机通过。

## 证据保存要求

代码/配置、实际命令、时间、开发机或设备身份、结果、限制和产物指纹应放到新报告，旧证据不覆盖。
原始视频/模型继续留在运行产物目录；仓库只保存必要摘要和哈希。`/tmp` 路径可能消失，历史hash不能保证文件仍可取得。
