# 当前验收与现场边界

## 2026-09-18：常驻SDK执行层与会话隔离（未部署）

新增executor_service、RemoteStationIO及可选--executor-socket，默认旧路径不变。四臂SDK由独立service持有，原StationIO映射在上层复用为纯目标收集，完整四臂目标一次提交后按seq确认，不是四次独立网络写。验证租约超时不关闭SDK、旧会话拒绝、旧seq/过期时间拒绝、完整校验前不部分下发、独立stop锁存/解除不恢复旧租约、显式支撑release、Unix真实进程PID跨客户端重连不变。强杀上层客户端后等待看门狗，新客户端仍附着同一个owner；完整Runtime通过代理运行HOLD无错误，Follower点动目标未被HOLD覆盖。

最终全量397通过、3跳过、17子测试通过，包含补充的序列/时效边界回归；未测试真实CAN/负载下的保持与往返延迟。本轮未连接IPC、未部署、未重启或运动，用户继续原现场测试。首次迁移需受控关闭旧SDK，之后上层更新不应释放力矩；owner自身更新仍需安全停机。录制活动集不保证跨会话重启无损续写，不能称整个系统无中断热升级。

## 2026-09-18：HIL自动跟随原生增益对照（待部署）

用户要求Leader自动跟随使用与Follower一致的位置控制增益，人工再回手动模式。StationIO仅在HIL镜像且source=policy、无维护覆盖时采用interaction_rules.POLICY_GAIN=1.0；适配器使用原生Kp/Kd，不改目标、频率或人工映射。YAM原生Kp=[80,80,80,10,10,10]、Kd=[5,5,5,1.5,1.5,1.5]。RESUME仍旧低增益有界对齐，介入/回零40%和手动重力补偿不变，纯推理Leader仍手动。不能把增益相同当作物理同步已通过。

193fb28已获准部署，Web/设备/回放服务active，随后用户自行运行。上一版热重载只接入HOLD_GAIN，未接入自动跟随增益；此次补齐StationIO入口仍须受控设备部署，之后POLICY_GAIN可HOLD热重载。本轮未部署/重启或运动，真机同步改善尚未验收；此前P95左0.2852rad仅作对照基线。

## 2026-09-18：交互热重载、锁定交还与回放拒绝（离线）

此前b2a6124已获准部署并重启，随后只检查服务active/设备disconnected，未由助手发起运动。用户之后测试反馈手动偏重、第二次按钮不应自动交还，以及回放姿态拒绝。核查SDK源码：`enter_gravity_comp_idle`会用`_grav_comp_kd`覆盖实际命令阻尼；YAM配置为[0.1,0.1,0.1,0.3,0.05,0.05]，故先前“kp/kd均为0”的描述只适用于配置字段，不代表实际下发Kd。手动无位置Kp、仍有官方阻尼/重力与摩擦补偿，不能仅凭代码断言体感根因。新增状态leader_control读取真实命令Kp/Kd/manual/摩擦开关，不盲改补偿或阻尼。

第二次手柄①改为handback_hold，保持四臂各自当前姿态、高增益Leader锁定；之后页面交还才开始RESUME。交互规则和保持增益移入可HOLD热重载模块，仍由唯一设备线程执行，SDK/急停不热替换。回放差异>0.2rad保留拒绝，但服务返回结构化replay_refused，客户端锁定HOLD并提示、游标不推进、不再因此进入不可恢复FAULT；不会偷偷跳到原录制下一姿态。须安全对齐或切换模型来源，回放不是会根据人工改动重新规划的模型。新回放协议需服务和客户端配套更新。

本轮全量391通过、3跳过、17子测试通过；覆盖锁定不自动交还、重载保留SDK对象/目标/游标、回放拒绝不FAULT。只离线修改、未部署或重启，真实阻力与动态锁定尚未验证。规则重载只解决该模块后续迭代，不声称所有底层改动无需重启。

## 2026-09-18：主从同步复核与两阶段介入（待部署）

最新完成录制为IPC `/data/YAM/data/episodes/5dbcd16f-6d45-4e60-9791-10cdbe6f1191/session_20260918_111928_c32ea8/episode_000001`，1318帧、aborted、error为空；policy1075帧、human240帧、takeover1帧、resume2帧。POLICY同帧反馈的六关节最大绝对主从差：左P50/P95/max=0.1534/0.2852/0.3796rad，右=0.1118/0.1615/0.2884rad；左J2/J3 P95为0.2527/0.2852rad。尚非精确同步。这是位置差而非纯延迟；人工相对遥操作不以绝对主从相等判错。反馈先于本行下发，不能把同帧目标减反馈当作准确电机延迟。

介入改为持续TAKEOVER：冻结四臂各自当前目标，Leader Kp原生40%、Kd不变；右Leader①新按下沿才解锁至既有手动重力补偿（位置kp/kd为0），重建相对偏移、保留夹爪软接管。左①不解锁、持续按住不连跳；人工①仍交还策略。增益保持不是实体急停或绝对不漂移保证，实际效果待验收。本轮未改策略跟随增益。

来源新增本地回放`ws://127.0.0.1:8002`与Thor`ws://192.168.250.1:8000`快捷填入；HOLD时显式切换，不自动换模式/运动，沿用独立通信子进程。核心/runtime/RTC/按钮61项、追加按钮与规划进程回归54项通过（有重叠），JS语法及改动文件Ruff通过。未部署或运动，启用须获得本次受控重启许可。

## 2026-09-17：无跨块融合对照（已部署，待真机推理验收）

按操作者要求先观察原始策略输出，不再默认使用整段重叠的TDA融合；本站配置改为`raw`，保留原有单在途异步预取、观测时间对齐、夹爪[0,1]限幅、关节3rad/s反馈相对目标包络及30Hz直接下发。`raw`新块接管时关节与夹爪均只来自新块，不加入短过渡或100Hz二阶滤波。已接受回复的录制记录新增裁掉步数和同一目标时刻新旧计划的最大关节/夹爪差；这些是目标差，不是电机实际跳变。模拟150ms回复覆盖控制不断流、前缀裁剪、新块接管和记录字段；相关专项39通过，全量345通过、3跳过、17子测试通过。操作者确认四臂已支撑、有人现场照看后，IPC `/data/YAM`快进至`51042a9`，受控重启`yam-device`和Web。重启后只读页面状态报告四臂及三相机在线、HOLD、未录制、`policy_fusion=raw`、`policy_trajectory_hz=0`、策略通信ready、无控制错误；期间未由本次操作启动推理或发送运动命令。此为部署/静态状态核验，尚非本轮真机推理效果验收，不能判断抓取或换块跳变。

## 2026-09-16：推理模式收束与IPC恢复观察

操作者判定多块`ensemble`的真机效果不理想，纯100Hz线性插值的短测也出现明显异常动作；后者已在`d503684`回退。本轮删除多块融合的动作历史、权重、CLI参数及测试分支，只保留最新块`raw`与可关闭的两块短接缝`smooth`，夹爪始终取最新块；100Hz策略专用二阶轨迹仍在。`smooth`各窗口尚未被操作者认可为最终效果，不能把代码保留等同真机通过。本地全套338通过、3跳过、17子测试通过；清理代码时未发送机械臂运动命令。

IPC在18:27这次启动后`/data`恢复挂载；18:41三台D405相机独立连接并显示约30fps，18:42断开。18:43:21首次四臂连接报`can_right/can_lead_l/can_lead_r`未UP；18:43:53再次连接、18:44:01进入HOLD。18:44:39三相机连接；18:45:45断开会话时报`SDK state update stale`，不能仅凭页面事件判定过期首次出现的精确时刻或根因。18:46:07重新连接四臂，18:46:23–18:46:37有一次短策略运行，18:48:33会话结束并报告MP4/HDF5已保存；未从该页面快照验算帧完整性或抓取质量。最后读取8766`/status`为机械臂/相机均断开、无控制错误，不能推断未来CAN、NVMe或SDK反馈已稳定。此前间歇性`fe150000.pcie`链路失败、USB-CAN异常与相机启动附近的状态龄故障尚无共同根因证据；后续应采逐臂状态龄、控制线程调度、相机启动时序和内核日志，不放宽0.25秒保护代替诊断。来源：IPC 8766`/status`事件及状态快照；18:45日志细节未在本轮取得。

对最后一段`session_20260916_184600_d342f5/episode_000001`的417帧、13.87秒HDF5读回：412帧有策略动作，39个新块，运行设置为`smooth/10步`、100Hz`second_order`。每块实际执行7–12个30Hz采样，中位11个；10步过渡几乎覆盖整个换块周期。将记录的策略选择与对应Thor原始50步块逐项比对，接缝后目标相对新块原值的最大关节差中位0.105rad、p95 0.412rad、最大0.699rad；第5模型步中位偏差0.247rad，到第14步才归零。它证明10步配置明显改变策略轨迹，不能称为无损平滑。原始选中目标逐帧最大关节变动p95 0.217rad、最大0.656rad；接缝后分别为0.068/0.100rad。大于0.02rad的相邻单步反向计数由49降到8，但不能仅凭这个计数判定抓取更稳：执行慢于模型目标时闭环会继续重规划。100Hz二阶trace无丢项，滤波目标相对输入目标的最大关节误差中位0.064rad、p95 0.110rad；实际机械臂运动还需结合反馈。两层都可能贡献滞后和摆动，记录不支持把原因单独归给二阶，也未做同任务、同初始条件的模式对照。录制原文件仅留IPC，不把大数据复制进Git。

较早的`session_20260916_172745_b3d04e`五段共5000帧中，原始Thor目标与选中目标的最大关节差p95为0.260rad、二阶输入与滤波输出差p95为0.114rad，也显示两层改写并存。会话元数据的默认`smooth_steps=4`不能证明中途页面没有热调整；样本尚未逐帧记录生效窗口，因此不把此会话当作严格的4步对照。

对之前失败的纯100Hz线性段`session_20260916_175434_7914ee/episode_000001`重算真实写入间隔：775条写trace无丢项，中位间隔9.99ms，最短0.268ms；两次约0.28–0.31ms间隔仍各改变0.03rad，按墙钟相除约97–106rad/s，远非名义3rad/s。原因是实验性线性实现按固定10ms步长更新，但写线程赶上调度时可能连续突发；0.03rad/名义步不能保证真实时间的速度。操作者的异常动作与该缺陷相容，不等于每一次大幅摆动均由它造成。当前二阶记录仍有最短0.30ms写间隔，但对应最大步长约0.006rad；调度器真实时间约束尚待修复，不能把回退称为完整安全保证。

## 2026-09-16：策略执行分层时间记录（仅离线）

为下一轮夹爪/手臂配合诊断，在不改变融合、限幅和运动参数的前提下，30Hz样本新增`policy_selection`：同一目标时刻的关节候选请求ID、观测时刻、模型索引及融合权重，以及夹爪实际选用的请求ID/观测时刻/索引；`bounded_action`与`bounded_at`保留经两级反馈相对包络后的目标。100Hz写线程每次在有界64项RAM环中记录收到目标的时刻、SDK调用起止、逐臂SDK调用起止、滤波目标、速度状态及SDK提交目标，30Hz样本以`policy_write_trace`取走并显式报告丢失项。`submitted_at`仍是30Hz快照，并非每次下发的时间；SDK调用返回也不等于电机已到位，须结合后续反馈判读。本地全套335通过、3跳过、17子测试通过；IPC提交`6ffd8c2`，四臂断开下专项45通过，三相机重连后30Hz健康。未有真实抓取归因证据。

## 2026-09-16：录制owner IPC三相机短测

提交`fd96a2d`部署后，在四臂已支撑并断开、四CAN DOWN的条件下重启`yam-device`与Web；录制进程/编码/FFmpeg实际落在CPU0–3，设备控制进程保留CPU4–7。仅三台D405的90秒30Hz、640×480、`h264_rkmpp`测试提交2692帧，HDF5与三路MP4分别完整读回2692帧、两段提交、积压峰值传输1/会话1/编码0；结束后的保存约2.4秒，NVMe原图临时峰值约4.99GB并最终排空。设备页相机已重连，四臂保持断开。本结果不证明8小时持续录制、运动中控制丝滑或抓取成功，不能用无臂相机测试代替现场对照。

## 2026-09-16：10步异步重规划与控制/Web进程拆分

本站推理关节目标包络先从5.0rad/s降至2.5rad/s完成首轮真机短测，再按用户体感降到1.5rad/s；2026-09-16后续短测发现长期命中反馈相对目标包络会形成追赶，用户决定放宽到3rad/s。该参数不是底层真实速度或加速度限制；上游同步分块客户端不能直接替代本站非RTC路径。正常重规划间隔约为10个30Hz动作（333ms），缓冲截止时间仍可按最近16次“observation参考时刻→动作可用”p95加67ms余量动态提前。

[150ms模拟延迟测试](../tests/test_async_inference.py)在动作约第10步发起唯一在途请求，等待期间旧计划连续产生policy动作；新50步块返回后按观测时钟裁掉前4步，首个执行索引为4而非0。新旧观测网格不锁相时，旧关节计划在新块精确目标时刻线性插值后再做短窗口融合；不外推，夹爪仍取新块。状态新增当前动作索引、裁剪步数、融合前边界差值和速度包络介入标志。

常驻设备owner与Web/API已拆为`yam-device.service`和`yam-workstation.service`，二者通过仅本机Unix socket通信。进程级mock测试中设备已连接运行后重启Web，设备PID保持不变、控制tick从9继续到25；新Web附着后设备由HUMAN进入HOLD，没有关闭SDK。此项只证明本机mock进程生命周期，首次部署到IPC仍需一次停止旧单进程服务，真机上“Web重启不释放力矩”尚待现场复核。

随后提交`e729ded`部署到IPC：首次迁移按计划停止旧单进程并释放四臂一次，再启动常驻device与Web服务。断开状态重启Web时device PID保持56485；重连四臂和三相机到HOLD后再次重启Web，device PID仍为56485、状态保持connected/HOLD，控制tick从1473继续到1556，没有重建SDK。用户随后自行开始约6秒推理并停止，共记录203帧、19个回复；请求RTT p95约136.7ms，观测到动作可用p95约166.1ms，无控制故障。用户反馈2.5rad/s仍偏快，本站改回与上游默认一致的1.5rad/s，尚待重启device生效。

该真机段夹爪合同方向得到端到端证据：`0=闭、1=开`；右夹爪策略目标从约0.964降到0.735，最终提交最低0.764、Follower反馈最低0.769，说明方向与物理映射生效。左夹爪策略目标最低0.931，基本保持张开。Thor完整50步块的后半段多次出现更低闭合值，但每次重新观测后的近期目标仍偏开；控制侧没有把未来闭合提前执行，也没有对夹爪做temporal平均。用户体感“没有夹”不能归因于映射反向，主要是当前模型近期目标未要求明显闭合；任务效果仍未通过。

## 2026-09-16：官方零位回位首轮真机诊断

用户确认四臂支撑、当前到零位路径清空并允许一次服务重启后，IPC部署`factory_zero_home`并重连四臂；运行状态为HOLD、无故障、四个手柄按钮释放，页面状态确认官方六关节零位可用。首轮回零进入`homing`约12秒，但Follower/Leader反馈均未变化，夹爪保持约0.949/0.971；系统随即请求HOLD并确认维护状态回到idle，无故障。

原因是旧维护插值每周期从实测位置只前推0.004rad（0.12rad/s÷30Hz）：真实电机受负载/静摩擦未响应这个小误差时，下一周期又从相同反馈重算，目标无法累计。修复改为与i2rt `move_joints`相同原则的起始姿态—目标时间插值，并在规划与反馈差超过0.15rad时停止，避免受阻后积累过大目标。带0.01rad死区的离线回放已证明目标可跨过死区，阻挡模型会停止；维护专项6项通过。该结果只证明首轮“未运动且已安全取消”和修复的离线行为；第二次部署及真实到零尚未执行。

第二次部署重连时，左Leader的J3/J4从约0.46/−1.03rad变化到接近零，发生在回零事件发送前；现场用户立即指出，软件随即锁存停止。变化来自服务重启失能及Leader重新进入重力补偿/保持的连接阶段，不能归为Follower回零成功。由此纠正范围：固定零位只控制两台Follower，Leader保持手动/重力补偿且不接收位置目标。修正后的Follower专项离线测试通过，真机尚待重新执行。

Follower-only版本部署后再次执行：两台Follower最大关节偏差从0.641rad降到0.059rad附近，Leader未进入位置控制且最大读数保持约0.165rad，Follower夹爪始终约0.949/0.971。残差集中在两侧J4（约−0.059/−0.057rad），其他关节大多在±0.02rad内；旧0.015rad反馈完成阈值最终报超时，已明确结束并保持。后续完成语义对齐官方`move_joints`：时间插值结束、跟随误差不超过0.15rad即完成并维持最终零目标，不提高增益或持续顶住等待编码器精确为零。该语义修复已离线通过，尚未重启进程复测。

2026-09-16再次真机操作暴露出时间插值仍会在正常负载下跑在反馈前方：规划—反馈差超过0.15rad后约2–3秒暂停，操作者多次点击才分段接近零位；最后两侧J4仍约−0.064/−0.061rad。修复改为反馈治理轨迹：0.08rad跟随窗口内按0.12rad/s推进，落后时冻结规划而非继续积累；完全停滞3秒或已提交规划异常偏差超过0.15rad才报出具体侧/关节，最终零目标实际进入提交路径后才结束。正常、慢反馈、完全阻挡和小残差专项测试通过；因机械臂安装调整断电，尚未重新真机验收。

同轮190帧、18个回复的真实推理记录显示：热态请求约121–131ms、Thor约110ms、控制工作p95约1.47ms，裁掉4–5个过期动作且未耗尽缓冲。相同目标时刻的相邻原始块最大关节差中位数0.316rad、p95 0.567rad、最坏0.943rad；12个关节1298/2280个采样命中1.5rad/s反馈相对目标包络。离线反事实回放的明显方向反转由raw 103次降至smooth 59次、三块同目标时刻ensemble 50次；这只用于定位块间不一致和追赶，不等于ensemble真机通过。

配置放宽到3rad/s并部署后，用户在页面完成188帧、18个回复的短推理：无缓冲耗尽或控制故障，随后新版反馈治理回零首次约23秒、近零位置第二次约2秒，均正常结束且未再触发旧的2–3秒误停。推理限幅命中率从上一轮56.9%降至43.0%，但明显下发方向反转从60增至126、实测速度p95从0.45升至1.00rad/s；本轮相邻块同目标时刻原始差最坏1.258rad，证明单独放宽包络会更直接传递块间不一致。

随后启用100Hz轨迹执行的1325帧真机段中，控制工作p95约1.94ms、无控制故障或缓冲耗尽；明显下发方向反转由此前125次降为0，实测速度p95约0.194rad/s，提交目标相对反馈误差p95约0.045rad。该段最终因录制外层RAM队列达到300项而HOLD，非人工停止；三路视频与HDF5均提交1325帧，但episode按失败语义标为aborted。采集30Hz而整段写出平均约23–24Hz，说明瓶颈在录制/编码持续吞吐，不是动作缓冲。300项是代码设置的约10秒保护容量，不能靠无限放大支持长录制。

同段夹爪复核：Thor合同及YAM端到端映射均为0闭/1开。最新50步中右夹爪约从0.94降到0.15，但频繁重规划下“始终取最新块”使实际选择最低约0.507、提交最低约0.671；按同目标时刻改用最早仍有效且不平均的夹爪预测离线重放，右夹爪约47.6%时刻低于0.5、最低约0.136。原生YAM和ABC-DiT均使用连续夹爪，未发现通用二值化；据此修改已通过相关离线测试，尚待重启后的真机任务验证。

同日再次点击回零在左Follower J4规划—反馈差0.081rad处被旧0.08rad窗口判停。窗口已调整为0.12rad、停滞观察由3秒延长到5秒，0.15rad硬异常保护保留；维护插值不再经过推理速度包络二次裁剪。正常滞后、慢反馈、堵转专项测试通过，尚未部署真机复测。

部署后第二次纯推理又在1370帧处达到同一300项队列上限；三路MP4与HDF5仍各提交1370帧，进一步确认不是偶发启动尖峰。用户确认推理rollout本就需要录制，保持推理与DAgger/HIL从运动启动自动记录、遥操作不录制、数据采集显式启停；故障必须通过真正的录制吞吐修复解决，不能关闭推理记录规避。

真正录制链路随后把固定32帧共享RGB环改为episode内NVMe临时spool：桥接线程先完整写临时文件并原子改名，编码子进程只接收小文件名，严格按序写MP4/HDF5，分段提交后删除对应spool项；停录消息排在所有帧之后。磁盘低于既有保留空间即显式失败，不把无限时长建立在RAM上。80帧快速积压测试证明生产端可先完成、编码端随后排空，且未封段前原图保留；90帧640×480三相机突发最终HDF5顺序完整、临时目录删除。服务配置同时保持控制CPU4–7完整可用，仅将录制/编码/FFmpeg迁到CPU0–3；尚待IPC三相机长时压力和运动中真机验收。

进一步本地拆分：设备进程保留非阻塞有界提交与低优先级传输线程，录制会话、NVMe暂存、分段清单与编码器改由独立子进程拥有；子进程异常可由设备侧发现并触发既有HOLD，不能静默报告保存成功。90帧640×480三相机跨进程突发已核对三路MP4及HDF5各90帧且顺序正确；无录制的遥操作会话可跨进程迁入任务目录而不重建机械臂；活动录制中杀死录制进程，模拟控制循环继续运行并进入HOLD。全量离线回归334通过、3跳过，后续小改的专项回归通过；IPC长录制与运动体感仍未验收。当前录制进程仍随设备会话启动/结束，不宣称已具备活动集无损热重启或独立systemd部署。

在真机启用前，100Hz二阶轨迹生成器曾保持与电机IO完全断开。对188帧记录使用3块同目标时刻ensemble、3rad/s、30rad/s²、临界阻尼频率10rad/s回放632个高频点：大于0.15rad/s的方向反转为0，命令速度p95 0.841rad/s、加速度p95 6.99rad/s²、相对融合目标滞后p95 0.177rad。该历史影子结果随后用于限定首轮真机参数；真实结果见上段，仍未证明任务成功率或长时温升。

页面恢复逻辑专项随后修复两层阻断：前端不再因`recording_error`禁用设备维护，Runtime入口和30Hz事件过滤均允许HOLD下的home/gravity通过；急停锁存、非HOLD、正在录制和维护占用仍阻断。按钮现在显示具体阻断原因，维护错误在hold/reset后清除；带模拟录制故障的急停→解除→home路径已测试，未连接真机复测。

提交`b022335`经用户确认支撑后部署并重启；随后以`initialize=false`普通模式重连四臂和三相机。现场状态为`teleop/hold`、`initializing=false`、`taskless_teleop=true`、无维护/录制/控制错误，页面实际显示新建与切换任务可用；未选任务时仅遥操作可用，其他三模式明确提示先选任务。未创建测试任务，故“任务绑定后四模式开放”仍由mock流程测试覆盖。当前systemd单进程同时拥有Web与设备SDK，Python后端重启确实会关闭SDK并释放力矩；静态页面热更新不会。

## 2026-09-16：IPC部署与真实Thor无电机协议探针

IPC在四臂和相机均断开时快进到`6a37dbe`并重启8766用户服务；直连路由为`192.168.250.1 dev lan1 src 192.168.250.2`，Thor健康检查返回OK。IPC真实OpenPI客户端用三路480×640 HWC RGB uint8黑图、14D零状态及英文prompt请求一次，得到有限`(50,14)`；客户端打包7.917ms、send调用14.727ms、等待回复115.142ms、解包0.099ms，请求2,765,185字节。夹爪预测最小`-0.000089`、最大`0.956152`，现有限幅会裁剪负越界。又从三台在线D405的面板JPEG预览解码为三路360×480 RGB（预览缩放、非同步原始帧），同协议再次得到有限`(50,14)`，打包4.128ms、send调用7.966ms、等待回复110.907ms；随后仅断开相机。两个探针只证明传输、形状、有限性和相机视图可用；零状态及预览图不能独立证明动作rad/绝对目标语义、原始帧配对或运动效果，机械臂始终断开且未发送动作。

[面板急停HTTP路径测试](../tests/test_workbench.py)从`/event/stop`排队进入30Hz mock Runtime，验证锁存、姿态冻结及解除后仍保持。当前软件急停保持Follower位置目标并保持使能，不直接失能；Leader保持冻结目标，软件按钮不能替代实体急停。真实四臂急停和raw推理运动均待现场许可与有人照看的短测。

## 2026-09-15：非RTC异步推理缓冲（仅离线模拟）

[模拟延迟测试](../tests/test_async_inference.py)覆盖单在途最新观测、观测时间跳步、KAI0式旧100%→新100%短窗口平滑和ACT较早预测优先集合、夹爪独立、动态截止时间预取、超时/耗尽保持、切换/急停拒绝旧结果。320ms模拟Thor的2.2秒30Hz Runtime得到65 tick、0 deadline miss、第二次RPC期间9个policy tick、6次推理调用；400ms抖动模拟在正常新鲜度间隔尚未到时提前请求，返回块丢第0–3步。有限夹爪超出[0,1]在控制侧裁剪，NaN/Inf拒绝；[condapi普通WebSocket处理器无模型联调](../tests/test_condapi_wire.py)用源handler验证三图+14D+prompt往返和50×14返回。全套314通过、2跳过、17子测试通过（仅既有Starlette告警）。这些证明本机模拟/源协议，不代表真实Thor、RK3588控制延迟或真机任务效果通过。配置和现场闸门见[运行手册](hil_quickstart.md#非rtc推理与可选动作融合)。

3588本地YAML/CLI配置`action_dt`，第0步直接对齐observation参考时刻，不设额外偏移；不记录模型名称、后端、指纹、握手metadata或服务URL。Thor侧用户本轮反馈50×14绝对输出合同：左右6关节rad、左右夹爪0关/1开，Thor负责反归一化、关节delta还原和32D裁剪；控制侧关节按SDK硬限位、夹爪裁到[0,1]。该反馈已按范围写入[接口](condapi_interface.md#v1-接口验收合同)，不是本轮真实模型输出测量。

同日IPC无机械臂的三台D405短测：三路图像最新帧引用/状态整理30次，p50/p95/max为0.141/0.162/0.939ms；`openpi_client.msgpack_numpy.Packer`对实际三图+14D+prompt打包30次，p50/p95/max为0.747/2.313/3.589ms，中位请求2,765,227字节；最旧配对图像帧龄p50/p95/max为32.625/45.133/47.884ms。测量在IPC CPU4–7进行，相机worker在`finally`停止，四臂未连接、CAN DOWN。图像帧在相机线程中已复制，此处未计入曝光、USB采集、socket发送、真实Thor处理或磁盘负载；用户给出的约130ms是Thor端估计值，不能相加后冒充已测全链路延迟。

## 2026-09-15：同会话采集与手柄短集

IPC页面真机完成无任务遥操作HOLD首次绑定采集任务、不断开四臂切换collect并显式开始；左①开录、右①停录、左右②活动集弃集得到现场确认与后台结果支持。保留集的902步HDF5提交行/视频索引与三路MPP MP4独立解码帧数逐项一致，停录后保存队列排空。短按②未由约5Hz原始状态采样逐沿抓到；右Leader`0x50E`间歇无应答的部件原因仍未知，不能把它定性为按钮故障或过温。本日三项测试任务与数据目录已按用户要求删除，证据边界见[工作站现场记录](workstation.md#2026-09-15-右-leader-间歇无应答与手柄按钮部分实测)；长时录制、运动中跟手性与Thor模型仍需另验收。

## 2026-09-14：P3 四臂初始化与零重力通过（P3仍在进行）

按i2rt官方YAM/YAM Cell顺序，先完成四路独立CAN上1–6号关节电机逐个激活、读取和OFF；
四路均为1 Mbit/s、每个电机错误码`0x1`、温度28–34°C，未发送位置目标。随后用户将左
follower标准`linear_4310`夹爪闭合并清空行程，使用真实YAM＋夹爪模型执行自动端点标定
和零重力初始化。夹爪原始范围为`[0.62123, -5.04368]`（跨度约5.665 rad）；标定后10秒
最大关节漂移为J4约0.018 rad，其余不超过0.0046 rad。CAN 23211发/23211收，零错误、
丢包和bus-off；退出后1–7号电机均回复OFF，四路CAN恢复DOWN。

一次先行的`no_gripper`试验因实体末端仍装有DM4310，软件模型遗漏末端质量/惯量而产生
J2约0.90 rad漂移，不能作为重力补偿参数证据。该试验复现i2rt v1.2.4关闭CAN socket
早于控制线程退出的上游已知竞态；当前固定到官方PR #81提交`4b3d6b5`后未再复现。
该阶段未写电机零位、未发送全零Home目标。详见
[P3左臂证据](evidence/20260914-rk3588-p3-left-init.json)。

右follower随后按相同流程完成：夹爪原始范围`[0.07038, -5.03948]`（跨度约5.110 rad），
标定后10秒最大关节漂移约0.022 rad，CAN 18079发/18079收且无错误。两臂复用实测夹爪
范围、跳过重复标定后同时进入零重力，用户在腕部相机保持安装的现场条件下逐臂轻推，确认
均能基本停在松手位置、未见明显问题。结束后左右1–7号电机均明确回复OFF，四路CAN DOWN。
这支持当前末端负载下的定性零重力验收；精确末端质量、长时缓慢下垂仍未量化。

随后按官方Leader手册，在不激活leader关节电机的条件下各读取100次teaching handle：
左右释放位置分别约0.0066–0.0153 rad和0.0066–0.0131 rad，按钮空闲态均稳定为`(0,0)`。
两者接近官方“释放时约0”要求，不执行编码器重置；按钮按下转换仍待验收。

两台官方Leader随后以`yam_teaching_handle`、`bilateral_kp=0`同时进入主动零重力，未启用
Follower同步或位置目标。用户完成手动检查并确认未见问题；结束时两台Leader均完成零力矩
关闭，左右1–6号电机明确回复OFF，四路CAN均为DOWN。因此当前四臂零重力定性验收通过，
仍需验证手柄按钮按下转换、单对再双对低幅同步、方向、暂停/急停与准备位。

本次左右Follower实测夹爪范围已固定进`configs/station_hil.yaml`，避免日常连接重复撞限标定；
该范围适用于当前两只夹爪，设备或夹爪更换后必须重新执行初始化向导并复核范围。

设备页五步初始化向导已部署到RK3588提交`d40b838`。IPC只读预检实际识别四个稳定CAN接口和
三台配置D405，返回通过，检查后四路CAN仍为DOWN；mock页面已实际走到3/5且未要求采集任务。
初始化会话固定`collect/hold`并阻止开始、录制和切换模式，验收记录原子保存到本机；后续补齐“完成/退出向导→同会话普通遥操作”转换，避免必须断臂，任务绑定后再开放四模式。全量回归
265通过、2跳过、17子测试通过。真机页面中的3–5步尚未重复执行，本次不因页面部署再次激活机械臂。

设备平台LAN入口随后以提交`912105f`部署为用户级systemd服务，实际从另一台LAN客户端打开
`http://192.168.110.140:8766`并显示“真实设备 / 设备未连接”。服务只监听IPC固定地址，
Host白名单、同源控制和心跳保持不变；服务enabled且active，未点击相机或机械臂连接，四路
CAN仍为DOWN。当前用户manager的Linger为no，因此已确认当前登录会话常驻及登录后自启，
尚未声称无用户登录时也能开机启动。LAN改动后的全量回归为267通过、2跳过、17子测试通过。
随后完全通过该LAN页面点击“设备与调试→只读预检→连接相机”，向导推进到2/5；三路真实画面
均显示、各约30fps、当时帧龄约10–30ms，健康栏为3/3在线。相机保持连接供后续测试，未连接
机械臂，四路CAN仍为DOWN。首次从页面执行第三步时因页面链路未先拉起CAN而安全失败，没有
构造机械臂；提交`4f40c3e`补上1Mbps拉起、逐路UP校验和退出DOWN收尾后，再次由页面初始化
连接成功。日志明确记录左右Follower均`enable_gripper_calibration=False`，没有夹爪撞限扫程、
自动回零或启动遥操作；四臂当前处于`collect/hold`，三相机保持约30fps，四路CAN均为
UP/ERROR-ACTIVE且当时错误、丢包、bus-off为0。页面向导已到3/5，等待现场执行重力补偿检查。
但连接约21秒后，右Follower第7号夹爪电机返回DM状态`0xC`，i2rt控制线程停止，第三步因此
不能算持续通过；从页面断开后四个机器人均完成零力矩关闭，四路CAN均回到DOWN。源码审计确认
不是重复自动标定：固定i2rt PR #81仍包含官方draft PR #82 `e599e9d`描述的多圈夹爪绕回错误，
右夹爪开端`-5.03948rad`会在启动时被错误改动`-2π`偏移，而标定端点未同步，随后hold可能把
目标映射到物理开端外约1.17rad并持续顶住限位。达妙DM-J4310手册将状态C定义为电机线圈过温；
本次未保存原始温度字节，不能反推温升曲线。项目已收入该官方精确补丁并在缺补丁时于打开硬件
前拒绝连接；全量回归270通过、2跳过、17子测试通过。右Follower冷却并检查无卡滞前不再激活。
初始hold采样还见较多30Hz deadline miss，长时控制性能尚未据此验收。

补丁部署后由操作员再次授权通过IPC页面执行完整预检、三相机连接和四臂初始化：19:17:25至
至少19:18:59持续约90秒未复现原21秒处的右夹爪过温，双Follower仍明确跳过标定，四臂SDK
反馈约0.6–5.2ms，三相机约30.6–30.9fps，四CAN均ERROR-ACTIVE且错误/丢包/bus-off为0。
夹爪坐标初始化修复据此通过本轮受监督验证。随后定位30Hz deadline miss：四台i2rt设备的状态
读取原本顺序等待各自状态锁，而锁会跨越重力计算和同步CAN写；IPC内核又通过
`isolcpus/nohz_full=4-7`预留了RK3588大核，但用户服务实际继承在0–3号小核。现将四设备读取
并行化，并从i2rt原子发布的不可变状态做无锁拷贝，保留时间戳陈旧检测；命令写入仍由控制线程
顺序独占。真实三相机+四臂hold中，读取p95从优化前约77.35ms降至约0.8ms；持久服务配置
重启后的连续15秒450周期零超期，完整控制工作p95为1.335ms，写命令p95为0.181ms。
四路CAN全程ERROR-ACTIVE、零错误/丢包/bus-off。272测试通过、2跳过、17子测试通过。这证明
30Hz计算和IO已有充足余量，但仍需低幅真实遥操作、暂停/急停、长时录制和温升验收。

次日IPC重启后再次连接时，实体双夹爪均处于张开且触摸不热，但软件反馈均被裁为0.0；50帧
保持记录的夹爪反馈/提交目标也始终为0.0，随后左右电机7先后返回`0xC`。根因是DM4310重启后
可把同一物理角度报告在相邻`2π`圈，而固定的约`-5.04rad`开端未随当前圈整体平移。现已加入
固定端点的`k·2π`整段对齐（不交换端点、不改变跨度）、2%机械硬端点余量及缺补丁拒绝连接。
同次发现teleop在未点击开始时自动开录，MPP冷启动导致32行队列溢出；现改为明确开始运动后
才录制，并在任何控制结束/故障时先释放机械臂、再排空编码。离线273测试通过、2跳过、17
子测试通过；尚未真机复测，当前四CAN DOWN，控制器断电重启并受监督hold前不得继续遥操作。

本轮全量回归还暴露了采集会话内存完成状态早于`session.json`原子落盘的偶发竞态；现改为
先落盘候选episode清单、再公开内存状态。专项连续20次及最终全量263通过、2跳过、17子测试
通过。该修复提高断开前检查点一致性，不代表已完成断电恢复或长时采集验收。

## 2026-09-14：P2 三相机通过

IPC 主环境的 `pyrealsense2` ARM64 wheel 与系统 GLIBC 不兼容问题已通过同版本源码本地构建
解除；doctor 可在 Python 3.12 环境枚举三台 D405。top首次开流需设备reset才恢复，left最初
接在480M口时并发短测有间隙。用户将left换到5000M口后，三路项目worker严格30秒窗口得到
900/900/899帧、均无帧号间隙且约30Hz，当前三路短流闸门通过。预热5秒后的抓图已确认
top俯视及左右腕部角色与RGB颜色；30秒软件到达配对p99为15.83ms，0次越过40ms警告线。
原先面向x86/Orin的单进程串行三路libx264路径在RK3588上发生有界队列溢出：单线程配置
在64帧队列下仅写入183帧，放开x264线程后在默认32帧队列下写入259帧后仍失败。保持
单个录制子进程及有界共享环不变，将三个独立视频流改为三线程并行、每流一个x264线程；
真实10秒与30秒录制分别提交299和897帧，三路MP4解码帧数和HDF5行数逐项相等，outcome
均为success，默认会话队列峰值均为10/32。P2按真实RGB短录制的帧数、索引、颜色、配对
偏差和队列闸门通过；长时、温升、断电恢复属于后续P5/P8，不由本次短测代替。
随后已部署MPP硬编并保持上述libx264回退。硬编60秒完整回读1795帧/路、队列峰值9/32，
三个编码进程各约5.4–5.7%单核CPU，温区约58–60°C；20秒同条件端到端CPU时间较软件路径
下降约38%。5秒分段测试的150/150/59帧在HDF5与三路视频中一致，抽帧复核角色和RGB
正常。该结果支持当前硬编默认启用，但不代替数小时采集、异常断电与磁盘压力验收。
本轮没有启动CAN或构造机器人。
详细环境、数值和适用边界见 [P2调试证据](evidence/20260914-rk3588-p2-camera-debug.json)。

## 2026-09-14：产品原则与分层记忆写回

用户确认P0/接线完成；维护工作以设备与采集平台（含初始化、调试与维护）和数据集平台为交付核心，两平台要求LAN跨平台浏览器使用。本轮只更新设计、手册、共享memory skill与冷热分层，不改业务代码、不操作现场设备或开放端口。现有两个Web服务的localhost绑定/Host限制仍需后续修改，不能把交付要求视为已经实现。

检查结果见 [产品记忆检查](evidence/20260914-product-memory-check.json)，来源与用户约束见 [基线证据](evidence/20260914-product-memory-baseline.json)。文件预算、默认加载路径、一级/二级路由、新旧摘要链接、记忆record及skill格式检查通过；现场资料仍按来源复查。dataset-portable-20260911因产品文档依赖变化保留为stale，原指纹/证据不变，本轮未重跑其业务验收，不代表确认了功能回归。

## 2026-09-14：架构与记忆核查

本轮仅在开发机核查YAM/condapi本地owner、配置和关键协议代码，写回架构基线、模型接口、硬件/环境边界及记忆路由；业务代码、station配置、condapi文件和远端设备未修改。源仓库HEAD、文件SHA256、历史现场证据及发现见 [核查报告](evidence/20260914-architecture-audit.json)。

已将Gitea认证与仓库部署分开、板载CAN与旧USB-CAN假设分开、Thor离线W与真实网络服务分开、原始采集与显式转换统一到当前口径；旧摘要完整归档。架构与P0–P8任务是实施约束，尚未完成的自动模型合同校验/来源链、设备跨进程独占及真实双机验收明确列出。

本轮文档/记忆检查结果保存在 [memory-check](evidence/20260914-architecture-memory-check.json)。该检查验证文件链接、record结构/指纹与差异格式；不证明CAN/D405、磁盘、当前在线状态、模型物理语义或任务效果。历史失效记录保留审阅状态，不更新旧指纹冒充重新通过。

## 2026-09-11后续：采集与转换完全分离

用户明确要求录制后可上传服务器再转换。已删除自动转换队列、录制回调和界面转换按钮；工作台及无界面CLI退出均只保存原始MP4/HDF5/清单。独立`scripts/convert_lerobot.py`及uv脚本锁支持单集、会话、多会话目录，失败保留partial和结果报告，不上传、不覆盖来源。

[全量测试](evidence/20260911-offline-conversion-tests.txt)：224通过、2跳过、9子测试通过。覆盖录制退出不调用转换、移动原始目录后转换、不重复扫描集、批量失败隔离和输出目录保护。独立uv脚本环境安装9个数据依赖后转换1949帧成功，保持一个episode且packet-copy；[官方0.5.1回读](evidence/20260911-offline-conversion-reader.json)首尾及边界样本成功。页面已验证不再显示转换状态或重试按钮。

以下上一版的后台转换描述属于历史实现，已由本次用户决定替代；原始HDF5格式、恢复和视频封装路径继续保留。


## 2026-09-11：MP4＋HDF5 分段录制验收

当前原始格式yam_hil_v2：独立录制进程、共享图像缓冲、HDF5批量追加、60秒默认物理分段、逐集清单提交、恢复CLI、持久后台转换队列与UI状态。四模式控制语义未改。后续大规模汇总/传输尚未实现。

- [全量测试](evidence/20260911-hdf5-final-tests.txt)：221通过、2跳过、9子测试通过；最后错误信息保留调整的[录制专项](evidence/20260911-hdf5-final-recording-tests.txt)另记结果。覆盖跨段帧序与像素一致、空值掩码、进程被终止后恢复、低磁盘、旧JSONL、专家间隔、转换队列暂停/恢复和重复入队。
- [65秒模拟运行](evidence/20260911-hdf5-integration.json)：三路640×480、30Hz，7个物理段，1950条原始记录、1949个有效视频帧、一个逻辑episode、0次deadline miss。末300tick控制工作p95约0.76ms；不是全程p95、RK性能或一小时内存证明。启动无图像行保留原始追溯，转换跳过。
- [官方读取器](evidence/20260911-hdf5-reader.json)：LeRobot0.5.1＋CPU Torch2.7.1，首尾及分段边界附近的三帧时间窗口成功读取；完整连续集packet-copy，不二次有损编码。验证使用临时环境，不向工作站安装Torch。
- [浏览器采集回读](evidence/20260911-hdf5-ui-reader.json)：三集2056/37/68帧，均后台转换完成、三路640×480、14维state/action，英文任务指令保留；重新启动工作台仍能读取转换完成计数。
- 曾因两个工作台测试共用真实本地任务库触发队列互斥，已将测试隔离至tmp_path；保留[修复前失败记录](evidence/20260911-hdf5-tests-before-isolation.txt)，不是放宽文件锁。

限制：仅Linux开发机mock；未真实断电、RK3588一小时、真实CAN/D405、Thor模型或condapi训练端验收。恢复输出需要人工审核；视频参数不兼容时转换明确失败；没有保证断电零丢失。以下较早日期条目为历史证据，其架构描述以当前转换手册为准。


当前按用户最终确认的模式运行：HIL键盘i冻结介入、手柄①交还；采集①开始/结束、②放弃；遥操作/推理手柄不分配功能。

## 离线验收

当前全量回归 **200 passed、2 skipped、9 subtests passed**；随后新增夹爪漂移保持测试，最终维护专项 **13 passed**。本轮 [工作台验收](evidence/20260908-operator-workbench.json) 包含浏览器操作验证、启动不连接设备、软件锁存与解除后保持、回位控制权、重力补偿与夹爪保持、有界预览进程及API请求校验。此前 [uv入口](evidence/20260908-uv-cli.json) 和 [模式/数据](evidence/20260908-approved-modes.json) 报告保留为历史。

测试包含：

- 冻结周期目标、下一周期相对姿态接管、夹爪软接管、迟到策略隔离。
- 普通事件队列满时介入仍可生效、重复i不能交还、HIL手柄②无操作。
- 多段采集、结束保留、放弃删除当前段、不误删已保存数据、队列反压与写盘错误。
- LeRobot保留完整HIL阶段和事件位，Follower反馈/提交目标不与Leader输入混淆；三路视频索引、失败半成品、格式重建。
- 有界性能窗口、文档链接、证据指纹与历史记录审阅。

此前官方LeRobot0.5.1读取器在独立CPU临时环境验证模拟录制产物（本轮未重跑官方读取器），读取状态/动作/三路视频/阶段信号。该环境不属于工作站.venv，也不表示RK3588安装了模型栈。

浏览器实际模拟录制1082帧，结束后生成LeRobot v3.0；官方0.5.1读取器回读元数据及首尾样本，状态/动作14维、三路图像3×480×640，见 [回读结果](evidence/20260908-operator-reader.json)。新增RGB直方图统计与逐像素统计数值对照通过，减少离线整理重复扫描；不将离线优化当作控制性能提升。

## 预览隔离对照

[开关预览对照](evidence/20260908-operator-preview-ab.json)：开发机mock三相机，各15秒；两组各452帧，三路视频索引/帧数一致，0次deadline miss。关闭/开启预览时control_work p95约0.54/0.44ms，队列峰值均2；差异属于短测波动，不能推断开启预览提升性能。采用合成画面，不覆盖HTTP客户端负载或真实RK3588/D405长时竞争。最终细节变更范围见总报告。

## 录制性能

[15秒开发机模拟报告](evidence/20260908-approved-modes-performance.json)：三路640×480 RGB、30Hz，450个控制帧，0次deadline miss；会话队列峰值1项，读取到录制入队部分p95约0.72ms（不含睡眠和低频统计）。3秒预热后RSS约100.3MiB，结束约100.5MiB。

这是Linux x86_64开发机的短测，不是RK3588/D405/CAN实测，也不是长期内存泄漏证明。首次相机尚未就绪时控制帧会标观测无效，三路视频各449帧，与JSONL索引逐项一致。编码完成不等于硬盘断电持久化保证。精确指标和测量范围以报告为准。

控制/相机/推理/编码分工保留；LeRobot整理在设备关闭后执行，不能把离线整理耗时计成控制循环耗时。

## 现场仍需完成

1. 核对CAN、夹爪电机型号、三个D405序列号和手柄按钮位置。
2. 单对再双对低速核验方向、重力补偿、夹爪、冻结接管与交还。
3. 三D405/USB拓扑下持续录制，测帧龄、偏差、队列、控制耗时、CPU和内存。
4. Thor冻结观测核对checkpoint/norm/action_dt与输出单位，再接入HIL任务。
5. 在真实RK3588上重复长时录制和视频/LeRobot回读，判断实际性能余量。

不默认清零、不自动机械臂回零，不把mock/安装通过写成真机通过。

## 历史证据

[1c04c83基线](evidence/20260908-hil-runtime-v1.json)和[cfdc1ed旧按钮实现](evidence/20260908-collection-buttons-v1.json)仅供追溯，不能覆盖用户最终确认的新按钮定义。原始日志和旧哈希保留；失效记录标stale，不改哈希伪造继续有效。/tmp产物可能被清理，仓库摘要不保证历史视频永久可取。


## 2026-09-08 任务工作台改造

白色悟演智能界面、任务目录与相机/机械臂独立连接已完成离线验证。全量测试203通过、2跳过、9子测试通过；随后任务目录异常输入与独立生命周期专项5通过。浏览器创建乐高分拣任务，录制493帧并成功导出；三路原始视频各493帧，LeRobot任务文本与原始/导出任务UUID一致。断开机械臂后相机仍在线。证据：[全量测试](evidence/20260908-task-workbench-tests.txt)、[任务专项](evidence/20260908-task-validation-tests.txt)、[采集回读](evidence/20260908-task-workbench.json)。只使用模拟设备，不代表真机性能验收。


2026-09-09视觉优先布局：工作台专项13通过，浏览器验证任务详情、三路模拟预览、视觉放大/恢复及HIL模式专属按钮。后端不变，未执行新的真机或全量性能测试。[布局验收](evidence/20260909-capture-layout.json)、[测试日志](evidence/20260909-capture-layout-tests.txt)。


2026-09-09英文task与维护界面：全量211通过、2跳过、9子测试通过，测试包含英文task校验、旧目录编辑保留身份、原始manifest/LeRobot任务文本与中文元数据分离。浏览器验证旧乐高任务补填、蓝灰配色与四个78px维护按钮，未连接真实设备。[测试](evidence/20260909-english-task-tests.txt)、[界面验证](evidence/20260909-english-task.json)。


2026-09-09字号与全屏前端验证：1920×1080/2560×1440无横向溢出，26px页面标题、14px连接按钮，大屏设备页双栏，全屏按钮往返验证。只改前端，未重跑后端测试。[检查记录](evidence/20260909-fullscreen-type.json)。


2026-09-09系统应用风格：工作台与任务专项23通过；浏览器检查1920/1120布局、88px维护按钮与任务表单。[测试](evidence/20260909-system-style-tests.txt)、[功能边界核查](evidence/20260909-system-style.json)。页面实际连接后端，但真机配置占位和Thor联调仍未验收，软件暂停不能替代物理急停。

## 2026-09-11 数据集工作台

独立 8767 工作台完成 YAM 原始目录索引、分类/备注、集合合并、回收站、三路按需预览、快速/深度检查及多 task LeRobot v3.0 后台导出。
全量 `uv run --no-sync pytest -q`：228 passed、2 skipped、9 subtests，见 [测试日志](evidence/20260911-dataset-workbench-tests.txt)。
双任务 6 帧导出由官方 0.5.1 读取器回读，task 与三路图像正确，见 [回读证据](evidence/20260911-dataset-workbench-reader.json)。
浏览器导入 3 集已有 mock 乐高数据，逐集三路预览、检查与失败分类可操作；单屏布局在 2226×1286 视口下 page/viewport 等高，无整页溢出。
只验证本机模拟数据；未验证 RK3588 一小时运行、海量目录吞吐、真实传感器、跨服务器部署。LeRobot 成品导入、任意裁剪/自动视觉清洗、分布式任务未实现。

## 历史尝试：测试目录互斥

[失败日志](evidence/20260911-hdf5-tests-before-isolation.txt) 记录两个工作台测试在data/tasks/conversion_mock处因非阻塞文件锁报BlockingIOError。假设是测试共用真实任务目录；实际干预为两个测试传入各自tmp_path/tasks，未放宽锁，[后续全量日志](evidence/20260911-hdf5-final-tests.txt)记录通过。
该归因为现有日志和修改所支持，但独立的单变量对照未知；后来自动转换队列又被移除，故不能把历史通过解释为当前队列可用。再次出现互斥时，先查实际锁路径、持有进程和测试隔离；确认当前代码仍有对应资源、或目录/并发条件变化后才重试。不要删除活跃锁或恢复已删除的旧队列作为通用修复。

## 2026-09-11 双格式、目录性能和迁移

实现原始YAM/LeRobot v3.0视频格式识别、共享文件episode范围预览/检查；转换仅接受原始包。目录库WAL、摘要分页/游标/索引/计数和批量扫描，32MiB预览缓存与2解码并发，独立后台进程。迁移TAR包含相对定位、审核与集合，流式校验恢复，新机器可使用独立uv锁脚本。

- [全量测试](evidence/20260911-dataset-portable-tests.txt)：254通过、2跳过、17子测试；其后小幅收尾改动复跑[9项专项](evidence/20260911-dataset-portable-final-tests.txt)。
- [规模测试](evidence/20260911-dataset-scale.json)：10万模拟目录首屏P95约0.60ms、深页0.83ms；一小时32×32/108000帧测试视频三路末尾定位约10.92ms。函数级本机测试，系统缓存未清空，不能代表100小时D405数据或100GB全量吞吐。
- [独立环境](evidence/20260911-dataset-standalone.json)：PEP723锁定脚本启动服务，子进程成功导入2集LeRobot，不安装Torch/机械臂SDK。
- [浏览器](evidence/20260911-dataset-portable-ui.json)：原始/LeRobot混合列表、TAR打包/新根目录恢复、三路预览完成；1137×1286页面等于视口，无整页滚动。
- [官方0.5.1回读](evidence/20260911-dataset-portable-reader.json)：恢复后的6帧模拟LeRobot保留双task和RGB。

尚未远端传输/部署、RK3588压力或全量真实数据验收；LeRobot成品物理合并/子集导出、聚合分片、训练版本发布、Lance与断点续传未实现。源和迁移包/解包分别占用磁盘，不能把无内存全量加载误写为无磁盘成本。

## 2026-09-16 线性插值首启与状态过期

IPC代码`7e13fe1`经现场许可受控重启后，设备进程报告`policy_trajectory_mode=linear`、`policy_trajectory_hz=100`；页面连接四臂后处于HOLD，未录制、策略写线程未活动。17:53:01–02连接三D405并预热MPP时，内核对三个UVC口各报告一次`-71`完成状态；同一秒30Hz设备循环因某SDK状态龄超过`max_state_age=0.25s`进入故障。终止状态保留的上一正常样本四臂状态龄为`[0.123, 0.002, 0.123, 0.122]s`，**不是触发故障那一次的四个实际年龄**；当时未启动模型动作，不能归因于新线性插值。相机启动与故障同时发生是相关性，不足以证明USB、编码器或CPU争用为根因；需补逐臂极值及启动时序。

故障会话17:54:30由操作者结束后，SDK记录四臂零力矩，CAN随清理置DOWN；这不是先前CAN物理故障的证据。操作者17:54:35重新连接，随后任务/相机在线，约17:56只读状态为推理模式HOLD、三路相机约30fps、四臂反馈龄约1–5ms、录制关闭、策略写线程未活动。用户随后在页面排队start/hold/home；线性模式运动效果与故障复现性尚未验收。来源：IPC `journalctl --user -u yam-device.service`、`journalctl -k` 17:52:40–17:54:42，8766 `/status` 快照；这些是当时观察，不能替代下次现场安全确认。

## 2026-09-16 线性插值推理短测与回退

操作者在重新连接后执行约7.93秒推理并录制239帧，随后表示纯线性插值带来明显异常动作，要求撤掉并回退。原始证据是IPC `data/episodes/5dbcd16f-6d45-4e60-9791-10cdbe6f1191/session_20260916_175434_7914ee/episode_000001/segment_000000/samples.h5`，含234帧policy、5帧恢复/保持、775条100Hz下发trace、0条trace丢失。原始模型关节目标相邻30Hz帧最大跳变0.367rad；215/239帧至少一个关节被反馈相对3rad/s外层包络裁剪。纯线性轨迹下发单步最大0.03rad，但有两次相邻下发约0.3ms就再次提交0.03rad；固定0.01s积分步长没有限制实际墙钟间隔内的变化。名义3rad/s不能被解释为实际时间速度保证，二阶模式也共用此调度器，回退不自动消除调度尖峰。记录字段`constraint_mask`比较最终提交与原始目标，混入高频轨迹偏差，不能直接当作外层限速计数；上述215帧是用`selected_action`与`bounded_action`另行比较所得。单次短测和操作者反馈支持撤掉线性方案，但不能单独证明所有异常运动均由该方案造成。

同日18:11经操作者确认四臂已支撑、行程清空、有人照看，受控重启`yam-device.service`以加载回退提交`d503684`；服务恢复为active，新PID 73352，Web新PID 73357。18:11:57从页面仅尝试连接四臂并HOLD，未启动推理、未连接相机、未录制。连接失败：页面报`can_lead_l` 1号电机无应答，设备日志还显示`can_left` 7号电机无应答，随后官方SDK关闭机械臂、四路CAN置DOWN，最终`connection=fault`。18:12:02内核连续报`xhci_hcd ... Unknown event condition ... HC probably busted`；它与CAN失败接近但不能据此断定同一根因。回退代码已在IPC磁盘且设备进程已重启，**没有完成四臂HOLD或二阶轨迹运行态验收**；故障未排除前不反复连接或开启运动。来源：IPC 8766 `/status`、`systemctl --user status yam-device.service`、`ip -statistics link`及`journalctl -k` 18:11:57–18:12:02。

操作者随后要求尝试左臂USB重枚举。按已核实序列号，仅对左Leader的`5-2.1`及左Follower的`5-2.3`依次切换USB`authorized=0/1`，两者均重新出现为原`can_lead_l/can_left`，右侧两个序列号和CAN映射未变。18:14:19、18:14:37内核在两个设备解除授权附近仍报xHCI异常。页面再作一次HOLD回连时IPC网络失联；18:15:24观察到整机新启动，重启原因无证据判定，不能断言由重枚举或回连触发。新启动中`rk-pcie fe150000.pcie`链路初始化失败，NVMe未出现在`lsblk`，`/data`未挂载，两个服务因`/data/YAM/.venv/bin/yam-workstation`不存在而以203/EXEC循环失败。18:16后已停止两个服务避免空转；四臂连接状态无法由页面复验，不应宣称USB操作修复通信。需现场检查IPC电源/PCIe/NVMe及USB-CAN物理链路，恢复`/data`后先核对服务和四CAN，再另行决定是否进行有人照看的HOLD回连。来源：IPC `udevadm`/`ip link`/`lsblk`/`findmnt`、当前与上次启动`journalctl`、`systemctl --user`。

操作者重启IPC后，18:18:16这次启动曾成功枚举`nvme0n1p1`并挂载`/data`，设备与Web服务均active，四CAN仍DOWN、页面报告disconnected。随后系统在18:19:25留下最后一条上一启动内核日志，18:20:10出现新启动；页面回连点击发生在上一启动停止记录之后，因此这次重启**不能归因于该点击**，也没有可靠的四臂HOLD反馈。18:20启动再次报`fe150000.pcie`链路建立失败，NVMe不存在、`/data`未挂载；两服务又以203/EXEC重试，已停止。NVMe间歇枚举与左CAN无应答可能都涉及硬件/供电/总线，但目前没有同一根因的证据。继续真机验证前需先稳定IPC存储与整机启动。来源：相邻两次`journalctl --list-boots`及内核日志、`findmnt`、`lsblk`、服务状态。

## 2026-09-17 动作规划进程拆分（离线与IPC HOLD验证）

将普通策略的TDA队列融合及同步50步完整块规划移到`yam-action-planner`子进程；旧`raw`只留内部回归测试，不再是页面/CLI选项，RTC只保留禁用占位，不冒充普通协议。`yam-device`保留独占SDK、状态机、安全限幅和通用计划消费，工作线程等待Thor与规划回复，30Hz循环不等待。子进程仅允许在HOLD/未录制时独立重载；epoch、请求时效和计划来源在设备侧再次校验。模拟延迟测试覆盖控制tick不阻塞、旧回复丢弃、规划进程失联HOLD、raw/TDA与旧算法逐步输出一致、同步50步后等待、TDA规划过程中已消费动作不重放。专项49通过，全量355通过/3跳过/17子测试，ruff与前端JS语法检查通过。2秒TDA与同步mock各59 tick/0次deadline miss、Thor/规划均ready；该数值仅是本地模拟，不代表RK3588或真机。

2026-09-17在现场确认四臂支撑、有人照看并允许一次受控重启后，IPC从旧版本升级至`41048c7`，`yam-device`主PID为28524，Web服务重启后均为active。IPC ARM64专项测试9通过。页面连接四臂后进入`teleop/HOLD`，四路CAN UP、反馈龄约0.5–5ms、无错误；未连接相机、未启动推理运动或录制。HOLD下请求独立重载规划子进程：子PID从28765变为28807，设备主PID仍为28524，四臂仍连接且HOLD。再把策略从TDA切到同步完整块并切回TDA，两次状态均显示规划/Thor通信ready、HOLD、无错误。只证明进程边界和安全静止态；不证明电机运动时的跟踪、推理效果或长时间稳定性。此前旧会话在升级关停时出现录制Broken pipe/aborted日志，不能据此宣称旧片段完整保存，须单独核查。后续由操作员在页面做短时`tda_smooth`及`sync_hold`动作/录制对照；RTC仍禁用，待目标tick与已承诺前缀协议联调。

## 2026-09-17 同步推理反馈相对裁减与移除（待新版IPC运动验收）

IPC旧进程最新两集`session_20260917_133059_82a88e/episode_000001`及`episode_000002`的每行`details.policy_fusion`均为`sync_hold`，各175/156帧，策略有效157/143帧。每块模型50步完整下发，回复观测到可用约0.19–0.22秒；每块后约4个30Hz tick为HOLD等新块。记录中157帧里149帧、143帧里106帧的最终策略目标受到本站反馈相对包络影响，SDK层未再裁减；第一集右关节最大单帧模型目标与提交目标差约0.87rad。相邻模型步的关节目标变化最高约2.69rad/s，低于旧3rad/s配置，但反馈落后造成以实测姿态为中心的逐步裁减持续命中。该限幅会改写模型计划，不能从此次表现断言模型本身不去乐高。用户要求移除后，30Hz直达路径的仲裁层和下发前两处反馈相对裁减均删除，保留SDK关节硬限位、策略夹爪[0,1]限幅、故障HOLD、独立维护/点动路径；可选100Hz二阶通道仍关闭。离线专项61通过、全量357通过/3跳过/17子测试。现场确认四臂支撑、行程清空且有人照看后，IPC快进至`7883ce6`并受控重启一次`yam-device`；新PID 29768，两服务active。四臂重新连接仅HOLD，反馈龄约1–4ms、无连接/控制错误，策略限速状态为`null`且轨迹频率0；相机未重连、未启动新版推理运动或录制。重启后模式回到配置默认TDA，需由操作员在页面明确切换同步并开始短测，方能判断跟踪与任务效果。

## 2026-09-17 RTC跨IPC无电机探针

Thor 固定 `ws://192.168.250.1:8000` 握手声明训练式 RTC、H50/14D、30Hz、最大前缀10步。
3588适配器补齐 `observation_policy_tick == target_start_tick == committed_start_tick`；
本地严格协议测试6通过。IPC 从已提交的 `session_20260917_135052_606ef7/episode_000017`
第100行只读取得三路640×480 RGB、14D反馈及后续10行提交目标；本次探针未打开相机、
未调用设备控制接口，原有四臂维持HOLD。
同一观测对 `d=0/1/10` 各请求3次：9次均返回有限 `(50,14)`，`server_timing.rtc_used=true`，
前缀保持逐值一致。跨IPC往返1056.4–1070.2ms，中位1057.5ms；首请求之外为
1056.4–1058.8ms，Thor报告推理约1037–1041ms。右夹爪有超过1的有限预测，未来控制侧
仍须按既有语义限幅；数值有限和前缀一致不等于任务动作安全或成功。

RTC新块第0步对应观测的30Hz policy tick，训练最大已承诺前缀10步即333.3ms。
仅本次热态往返已约31.7步；加40ms接管余量至少需要 `ceil((1.0575+0.04)*30)=33` 步，
尚未包含曝光/采集与物理tick映射误差，超过模型允许的10步。结论：协议通，但当前Thor
延迟不满足训练式RTC实时接管；不接入设备循环、不真机运动、不用迟到动作冒充可用RTC。
待全链路p95降到10步预算以内并定义/实测物理tick后，再离线测完整提交前缀与HOLD/丢弃逻辑。

## 2026-09-17 RTC优化服务跨IPC复测

Thor 同一固定8000地址的新服务仍握手训练式RTC、H50/14D、30Hz、最大前缀10步。
IPC继续用上节已提交第17集第100行真实三路640×480 RGB、14D状态及后续已提交目标，
只做网络推理，未发机械臂运动指令。`d=0/1/10`各5次共15次均返回有限`(50,14)`、
确认RTC分支且前缀保持不变。首请求228.94ms；后14次跨机往返p50/p95/max为
217.46/219.09/219.64ms，Thor报告推理p50/p95约198.82/199.22ms。
请求约2.765MB，热态打包p95约2.32ms、send调用p95约14.64ms；不能把
`往返－server_timing`全部称为纯网络延迟。

另在原有相机连接/HOLD下只读采集36次页面状态，最旧相机帧龄p50/p95为
33.3/40.0ms。按用户指定不加余量，独立p95之和约259.1ms，
`ceil(0.2591×30)=8`，故离线预设`d=8`（8步约266.7ms）；仅约7.6ms
名义空间，且尚未联合测量观测曝光→结果可用p95、物理policy tick和运行中资源争用。
前节约1.06秒是旧服务测量，不能再当作当前热态速度；本节也不等于RTC已可真机执行。
当前RTC仅有离线协议适配器，尚未接到控制循环，页面继续禁用；后续须先通过
完整30Hz时间轴/真实已承诺前缀/晚到丢弃的模拟和现场静止态验证。

## 2026-09-17 RTC控制接入与新检查点复测

新检查点同地址`ws://192.168.250.1:8000`仍声明训练式RTC、H50/14D、30Hz、
最大前缀10。IPC只读第17集第100行真实三路RGB/14D和随后8行已提交目标，
连续13次无电机请求均返回有限`(50,14)`、确认`rtc_used=true`、前缀逐值不变。
首请求222.73ms，后12次跨机往返p50/p95/max为217.84/219.29/219.35ms；
Thor所报infer约198–200ms。按先前在线相机到达帧龄p95 40ms、无额外余量，
`ceil((219.29+40)ms×30Hz)=8`，初始`d=8`，但它不是联合全链路p95。

本地增加RTC时间轴：观测按帧**主机到达时间**映射最近控制tick，历史前缀取实际SDK提交目标、
未来前缀经同一SDK硬限位后锁定；回复仅在预定`N+d`接管，过期结果不顺延。
模拟覆盖前缀不变、硬限位、迟到丢弃、重置拒绝旧结果；RTC通信子进程在HOLD下
独立切换，普通/TDA/同步模式不受RTC前缀混合。219ms模拟RPC下`d=8`与`d=9`
各2.5秒、各10次回复均接收，65个policy tick，0 deadline miss；这只证明该模拟
负载，不能证明真机调度下`d=8`足够。本地全套368通过/3跳过/17子测试，
另有既有Starlette依赖告警；文档/记忆检查无错误。
现场确认四臂支撑、行程清空和有人照看后，IPC快进`43f4d6b`，IPC专项11通过；
在原会话四臂HOLD/未录制、三相机在线且无错误时，只受控重启一次`yam-device`。
新设备主PID38677，Web服务保持active；重启后页面四臂/相机先断开，重新选择原任务、
连接三相机和四臂，仅进入HOLD。随后在HOLD切至推理模式和RTC，`policy_ready=true`、
`rtc_delay_steps=8`、通信握手无错误、四臂反馈龄约1ms、未录制、未启动运动。
切RTC时设备主PID仍38677，证明本次通信子进程热重载没有再次释放四臂。
页面HTML已能返回启用的RTC选项。短时HOLD观察RTC接收/迟到计数均0（尚未发推理），
`deadline_misses=2`为重启后累计值，尚不能归因于RTC运动。
用户自己启动推理运动；在真机有了RTC运行记录前，不可称任务效果或曝光同步通过。

## 2026-09-17 首次RTC现场故障诊断与离线修复

操作员启动短RTC试验后页面报`invalid policy response`并锁存FAULT。只读检查该集
录制spool：首个请求`N=5238,d=8`在控制tick5248返回，已晚于预定接管tick5246
两步，按设计丢弃；本地往返约300.0ms、Thor推理约200.0ms。第二个请求
`N=5248,d=8`在接管tick5256返回，本地往返约233.1ms、Thor推理约198.8ms，
但右夹爪的4个有限预测高于1，最大约1.025。Thor合同允许这种数值由3588限幅；
控制端误先执行SDK严格目标校验，故障与CAN或SDK反馈过期无关。修复为先把夹爪
限幅到[0,1]再执行SDK限位/校验；对RTC前缀超时仍保持原来的丢弃规则。
首次请求迟到、第二次零余量，不足以估计长期迟到率，但说明`d=8`没有现场调度
余量；默认改为9，并允许HOLD时经页面调整1–10。故障后控制循环锁存退出，原
“暂停录制”命令不能消费；新增FAULT终结路径发送`aborted`录制停止。离线修复
及测试不等于RTC真机通过。

获现场重新确认四臂支撑、有人照看和行程清空后，IPC快进`b6b07f0`，IPC相关
10项测试通过，受控重启一次`yam-device`。服务及Web均active；页面显示四臂
`disconnected`、不在推理运动，HTML含新前缀输入。未自动连接/使能。旧故障集的
manifest仍为`recording`、steps=0，`.recording-spool`有19项，HDF5可读19行；
这不是已完成的数据集。旧进程停止时其录制/编码子进程被中断，新版的FAULT
收尾只保护之后发生的故障，不能倒补这段旧数据。原件保留，必要时按
[异常恢复](convert.md)单独生成recovered副本并审核；本次未执行恢复或删除。

## 2026-09-18 RTC与DAgger准备验证

只读检查IPC最新已保存`5dbcd16f-6d45-4e60-9791-10cdbe6f1191/session_20260918_093240_c22760/episode_000004`：9504行、约316.93秒，控制tick无缺号；6个已提交分段三路视频清单帧数均与HDF5行数相符（未逐帧解码视频）。逐帧全部RTC，开头9帧等待、其余9495帧policy；没有human/专家帧，不能视为DAgger真机验收。

900次回复中2次迟到1 tick并丢弃；worker往返p50/p95/p99/max为232.80/241.82/247.49/296.68ms。控制间隔p50/p95/p99/max为33.33/34.15/39.28/103.65ms，无缺号不代表无调度抖动。有效观测9430/9504；图像软件年龄p50/p95/max为31.27/42.14/100.56ms，三相机接收时差12.23/14.54/33.60ms；非硬件曝光同步。当前保留d=9，未据此承诺所有请求均准时。

发现并修复：清单沿用启动YAML的`rtc=false/tda_smooth`；现在Runtime按实际运行模式更新每集元数据，录制线程冻结开始时配置，异步排空不会被下一集覆盖。旧集保持原件。本站默认选择RTC。

离线回归覆盖RTC策略→冻结→相对人工→交还→策略在一集中保留、旧回复拒绝、新前缀从人工结束姿态构造、专家标记、配置快照和导出。94项相关测试通过。现场读取时四臂connected/HOLD、RTC d=9、未录制、无报错；未执行运动或服务重启。后续现场需验证Leader跟随、介入冻结、夹爪拾取、交还等待及专家导出；本条不代表这些真机验收已通过。

该集为inference，Runtime只在HIL向StationIO传`mirror=True`，因此不能用此集验证Leader跟随。该集主从12关节最大误差的p50/p95/max为1.696/2.124/2.727rad，与纯推理不跟随的设计一致；HIL的镜像误差检查和交还等待必须另测，不能直接放宽阈值冒充同步通过。

## 2026-09-18 HIL短测与固定回放准备

- 用户确认Leader长手柄路径间隙后，从页面启动HIL短测。`session_20260918_093240_c22760/episode_000005`仅38帧（9 resume、29 policy），随后自动HOLD；实时镜像误差达0.508888rad。Leader最大关节运动范围仅0.024414rad，Follower最大范围0.454337rad。用户也确认Leader没有明显运动，**四臂同步未通过**。
- 随后页面急停锁存；10次状态采样Leader位置范围为0，Follower约3.55e-15rad。这是自动HOLD后的静止验证，不是运动中制动距离验收。离线测试覆盖急停冻结双Leader和双Follower、退出手动模式并持续保持。
- 可疑代码：Leader镜像目标每tick相对实测位置限幅，0.5rad/s÷30Hz仅0.01667rad，增益比例0.2；可能形成跟随不足，尚未证明唯一根因。本轮未提高增益或放宽镜像保护。
- 用户要求停止使用Thor，先回零再固定回放。已通过页面完成Follower回零，最大实测关节残差0.050927rad；Leader未执行回零，随后HOLD、无错误、未录制。
- 新增本地`hil.replay_policy`：仅读取完成集的submitted_action，通过loopback OpenPI协议供现有sync_hold全块执行；无Thor、无第二电机写入者。末块补最后目标且不循环。此为分块诊断回放，不声称跨块无停顿。当前会话URL不可热切换，真机回放仍待支撑确认后断开重连；尚未做硬件回放。
- Evo-RL字段与离线导出映射见convert.md；本轮离线回归71项通过。新增回放源与导出改动尚未在运行设备进程生效。

### 本地回放真机复现

- 用户确认允许切换并在场准备急停。IPC快进到a8f8bb6，未重启yam-device；从页面断开重连至`ws://127.0.0.1:8002`，切换sync_hold。独立回放服务读取上述000005的38帧；无电机WebSocket探针逐项确认目标一致，起点最大关节差0.041581rad。
- 10:39:23页面开始，10:39:29页面急停，最终HOLD、stop_latched=true、无错误。新记录`session_20260918_103824_c92b5b/episode_000001`40帧。状态采样Follower最大关节范围0.544366rad，Leader仅0.002289rad，最大主从误差0.543984rad；用户确认“没跟随”。固定记录已复现，不能归因于本次Thor请求或模型新预测。
- 当前保留急停锁存，不再重复运动。Leader镜像限幅相对每次实测位置而非累计命令，以及0.2增益仍为待验证因素；未放宽保护。此次仍不能声称动态急停停止距离已通过。

### HIL共用关节目标修正（待真机验收）

- 急停保持状态只读监听两路Leader CAN，关节1–3实发Kp约15.995、Kd=5，关节4–6 Kp约1.954、Kd约1.499；不是零增益。此读数不代替运动阶段逐帧证据。原先0.5/30rad实测相对目标限幅使位置纠偏项约限于0.267/0.033Nm，可能不足以推动关节，尚不能排除其他影响。
- 用户授权尝试Leader与Follower使用同一份策略关节目标。StationIO策略阶段改为共用硬限位后的六关节目标，手柄夹爪不接收命令；原生PD增益不变，主从误差保护不变。RESUME对齐仍沿用原受限路径，急停/介入冻结优先，人工与纯推理仍手动。
- 备用100Hz通道Leader读取滤波后的采样目标，不能称为同频或总线硬同步。默认仍为30Hz直达SDK。需受控设备重启才加载新代码，未热注入运行进程；真机动作前通知用户。

- 后续获准部署f964c19并重启yam-device，10:52:44重连HOLD，尚未点击开始或回零。10:52:55连接相机/切sync_hold，10:52:56出现SDK stale并进入FAULT；日志记录can_right电机5、can_left电机4、can_lead_l电机5的loss communication及SDK线程退出。同秒三相机uvcvideo报-71。CAN接口仍UP且bus-off计数0；不能据此认定线缆、USB或CPU中的唯一根因。共同目标修正**尚未得到真机运动验证**，故障时不能把界面connected当作有效姿态保持。

### Leader独立回零与长回放

- 用户自行恢复后短回放反馈Leader会动；不等同于长轨迹跟随精度验收。新增home_leader互斥维护动作、独立确认按钮，面板名称改为「零位与重力补偿」。Follower目标保持不变，Leader夹爪不下发，沿用维护轨迹的速度/跟踪/停滞保护；移除StationIO对已治理维护目标的第二次实测相对限幅。完成后Leader回到重力补偿，故不是刚性零位锁定。
- 全任务清单按帧数排序确认最长集为093240_c22760/000004，9504行逐帧全部rtc，约5分17秒。回放增加显式--all，保留完整目标序列、末尾保持不循环；同步50步块间有等待，不声称等时无缝回放。代码与54项相关测试通过；本功能尚待部署及用户真机验收。
- 随后获准部署c115794并重启；先连接相机再连接四臂，HOLD无错误。页面已确认「零位与重力补偿」及Follower/Leader两按钮。loopback:8002源已改000004 --all，无电机WebSocket遍历9504帧与源逐项一致；起始Follower偏差0.04692rad。已设置sync_hold，未点击任何回零或运动，交给用户实测。

### 暂停回放与回零增益修正（待部署）

- 用户实测暂停/恢复会跳跃。代码确认旧服务按发送块推进50帧，控制侧HOLD清空未执行尾部，恢复从下一块开始。新增提交后确认游标，过期token不推进；回放暂停不消费尾部，回零重置起点，恢复校验姿态。
- Leader回零错误实读为右Leader J4落后0.121rad触发停滞。用户授权只提高回零增益：Kp原生20%→40%，Kd与0.12rad/s、跟踪窗口、停滞保护不变。修复set_manual_control仅按manual缓存而忽略gain_scale变化；退出回零时可恢复原20%。尚未真机证明该增益能解决到位问题。
- 加入HOLD/未录制来源热切换：Web/API经owner队列使旧epoch失效，通信线程重建子进程，不重建机械臂。源码地址切换不自动改变融合模式，不自动运动；回放进度重置。88项相关回归通过，另新增暂停7帧恢复第8帧端到端测试通过。全目录ruff发现未改动recording_process.py的旧导入排序问题，未混入修改。
- 获准部署6393067，设备与Web受控重启，长回放服务同步更新确认协议。IPC无电机wire验证重复请求0帧不推进、按7帧确认恢复第8帧通过。页面来源重连测试设备PID61672不变、通信子进程61936→62066，四臂仍connected/HOLD、无错误。未执行回零或回放运动；40%回零增益及真实暂停恢复仍待用户验收。
# 2026-09-18 DAgger 介入门禁与录制审查（本地改动，未部署）

- 后续用户确认支撑并允许部署，IPC 已快进至 af29203，受控重启 yam-device 与 yam-workstation；两服务 active，/status 为四臂/相机 disconnected、无错误。IPC 36 项相关离线测试通过；未连接机械臂、未启动运动。实际介入录制仍待用户验证。独立 SDK executor 未在本次启用，不能据此声称硬件解耦已上线。

- 只读检查 IPC 最新已完成 HIL 集 `5dbcd16f-6d45-4e60-9791-10cdbe6f1191/session_20260918_133753_b5425d/episode_000002`：1659 帧，HUMAN 464 帧，455 帧 expert_valid；9 帧人工观测无效，未纳入专家有效样本。14D 提交目标、反馈和 Leader 状态完整；相机时间字段、介入事件及编号存在。三路 manifest 各1659帧，MP4头可读，未做全视频解码。该集 outcome=aborted、无成功标签，不能宣称可直接训练。
- 修复 LeRobot 导出遗漏 `handback_locked` 事件映射（bit16）；不改变 aborted 默认排除规则，不修改旧集。
- 新录制省略介入入口等待段，仅保存区间审计；原 tick/time 保留。介入门禁前后端覆盖暂停后重新开始绕过；关节调试开放所有模式的空闲 HOLD，保留互斥和急停条件。尚未进行本轮真机部署验收。
