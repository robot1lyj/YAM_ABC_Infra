# 历史快照：2026-09-22 整理前

源版本：`0bc1be5`。以下包含不同阶段的旧约定，不是当前操作指引；当前内容见 [collect](../collect.md)。仅修正迁移后的相对链接，历史结论和测量不重新认证。

# 设备与采集平台：遥操作、采集与手柄

本功能属于长期交付的设备与采集平台；设备初始化、调试与维护同属该平台。后续需求与修复按 [产品交付原则](../dagger_architecture.md#产品交付原则2026-09-14)维护“连接→遥操作或绑定任务→采集/接管→保存结果”的用户流程；模型/底层改造同时检查受影响的用户流程。连接状态、能否操作、保存成败与恢复办法应直接可见，不另建临时采集入口。

硬件：2台官方YAM Leader、2台标准DM4310平行夹爪Follower、3台D405。按钮①/②对应SDK的top/second；2026-09-15真机读到四个位置，左①开录、右①停录，左右②的活动集弃集得到现场确认与后台结果支持。②短按未被低频状态采样逐沿抓到，不冒充逐包证据。启动设备时释放按钮；现场证据及右Leader间歇失联边界见[工作站](../workstation.md#2026-09-15-右-leader-间歇无应答与手柄按钮部分实测)。

## 新设备初始化向导

“设备与调试”页内置五步向导：只读核对station配置、四路CAN与三相机序列号；连接三路相机
并检查新鲜画面；在夹爪闭合且行程清空后顺序构造两对YAM；进入重力补偿检查双Follower和
双Leader；最后保存配置SHA、设备清单、相机角色及实际夹爪行程到本机
`data/workstation/initialization_real.json`。初始化连接不要求先创建采集任务，固定使用
`collect`保持路径，不自动回零、不启动遥操作、不写电机零位。完成后支撑机械臂并断开，或
选择正式任务重新进入采集流程。更换机械臂、夹爪、CAN映射、相机或station配置后应重跑。

## 模式和操作

| 模式 | 控制方式 | 按钮① | 按钮② |
|---|---|---|---|
| 遥操作 teleop | Leader控制Follower；仅验证设备，不录制 | 无功能 | 无功能 |
| 推理 inference | Thor控制Follower，Leader不参与 | 无功能 | 无功能 |
| HIL | 键盘i冻结策略并自动进入人工纠正 | 仅人工阶段交还模型 | 无功能 |
| 数据采集 collect | Leader控制Follower，手柄管理episode | 开始/结束当前集 | 放弃当前集 |

左右手柄都能操作，按下沿触发，每种按钮250ms整站去抖，同时按同类按钮只触发一次。采集模式同时收到结束和放弃时，放弃优先；没有录制时按钮②无操作。手柄按钮不启动机械臂、不切模式、不退出程序。

## 遥操作和采集

普通遥操作不需要创建或选择采集任务，也不要求连接相机：直接点击“连接机械臂”，系统会进入
`teleop`，保持当前位置，等待操作者明确点击“开始遥操作”。该会话不创建训练 episode，
界面不显示本集录制、结果标记、会话统计或最近采集；Leader 实体按钮也只检查输入状态，不能触发
录制。默认未绑定任务的遥操作会话保存在 `data/workstation/teleop_sessions/standalone-teleop/`，与训练数据隔离；指定会话输出根目录时，遥操作临时目录位于该根目录同一文件系统的兄弟 `workstation/teleop_sessions/` 下，便于首次绑定时原子迁移。
若要沿用已连接的四臂进入采集，先点击暂停使四臂保持，连接三路相机，在任务卡创建或选择任务，
再切换“数据采集”并明确点击开始。首次绑定会把尚未录制的会话迁入该任务UUID目录，控制线程、
四臂连接和力矩状态不重建；切换模式时仍保持，不会自动运动或自动录制。

正式采集仍使用以下流程：

```bash
uv run --no-sync yam-workstation --mock --mode collect --web-port 8766
```

1. 可先创建/选择采集任务，再分别连接相机和机械臂；也可从无任务遥操作进入保持后首次选任务，不必断开四臂。等待三路相机就绪，切换数据采集并按 `s` 启动。普通遥操作启动后按 Leader 绝对关节姿态 1:1 驱动 Follower；夹爪扳机保留软接管保护。HIL 人工接管仍使用相对偏移，避免策略执行中突然跳变。
2. 摆好场景，按手柄①（或键盘 `r`）开始录制，确认绿色录制按钮切换为结束录制，状态显示录制时长。
3. 完成示范，按①结束并保留；不满意则按②放弃。两者都不停止遥操作，便于准备下一次示范。
4. 每次新录制都是新episode。放弃的片段后台关闭编码器后删除，索引只保留discarded结果，不进入训练数据；不会删除前面的好数据。
5. 可用 `g/f` 标记成功/失败，未标记为unknown，保存不等于自动认定任务成功。
6. 空格单独暂停运动，取消排队运动指令。暂停/退出时未结束的采集标aborted；恢复用 `s`，录制需重新开始。
7. 结束录制后，支撑机械臂再点击“断开并保存”关闭设备。本集完成MP4/HDF5保存后即可开始下一会话；LeRobot由独立脚本在本机或服务器上显式转换。不带界面的CLI用 `q` 退出。

遥操作模式始终不录制；需要保存示范时先保持并首次绑定任务、连接三路相机，再切换到“数据采集”。
任务一旦绑定，当前四臂会话不能再改选其他任务；下一任务须先断开并完成保存，防止混写。
推理rollout与HIL启动策略后自动记录，模式切换先暂停并切开活动片段。

真实设备去掉 `--mock`；先按 [硬件事实](../workstation.md) 填写CAN、夹爪型号和三台D405序列号。构造设备可能施力矩并校准夹爪。空格是软件暂停，不是硬件急停。

## HIL / DAgger

HIL模型阶段，Leader镜像跟随Follower，因此手柄不能触发接管。键盘 `i` 是单向介入，不是接管/交还切换键。

- 接收到 `i`：废弃旧策略与在途结果，冻结Follower当前目标，并给Leader提交当前位置保持目标。
- 下一控制周期：Leader进入重力补偿，Follower由Leader相对于接管瞬间的位移控制，首个人工目标不因两者的小误差跳动。夹爪有软接管。
- 人工纠正后按手柄①：Follower暂时保持，重新取观测推理，Leader受限对齐；新策略和Leader就绪后恢复模型。
- 交还等待期再次按 `i` 可重新介入；手柄①在策略、冻结、等待阶段无功能；②全程无功能。

同一任务的策略、冻结、人工和恢复都在同一episode里。冻结是目标冻结，不是四台机械臂物理瞬时停稳的承诺。

## 数据与性能

`observation.state`来自与相机配对的Follower反馈；原始实时反馈另存。`action`是限幅后提交给Follower的绝对目标。Leader输入只用于控制和追溯，不能冒充Follower实测状态。

录制期间控制循环只向有界RAM队列异步提交；低优先级传输线程把原图及行数据交给独立录制进程，由后者有序写到NVMe临时spool。独立编码子进程和HDF5分段保存可在停录后继续排空，不要求与运动同时封装完成。控制侧队列默认约10秒容量（30Hz时300帧），录制进程另有有界会话队列；它们吸收暂存写入的短抖动，长期编码积压落在磁盘而非无限增加RGB内存。每段MP4/HDF5提交后才删除该段原图；磁盘不足或暂存写入本身追不上30Hz仍会显式中断，不能保证任意时长或突然断电时完整。录制进程失联或录制失败会请求全站保持，但正常录制仍与控制共享内存带宽和IO资源；CPU亲和性隔离需用IPC三相机长时吞吐及运动中p99验收。当前录制进程随设备会话启动和结束，不支持活动集无损热重启。工作台不执行LeRobot转换；界面显示录制与原始保存状态，详见[架构](../dagger_architecture.md#性能与格式整理)和[数据格式](../convert.md)。

原始会话保存全量状态/来源/策略回复/同步质量。LeRobot保留完整HIL阶段、人工有效标记、干预编号、事件请求/生效时间。观测短暂缺失时保留最近图像、用无效标志标出，不假装获得了新相机观测。

旧英文采集说明见 [归档](../archive/collect-1c04c83.md)，只用于旧工具。

## 准备下一次示范

需要摆放机械臂时，先结束录制、暂停运动。在“设备与调试”进入重力补偿，手扶四臂摆到准备姿态，结束补偿后保存准备位。需要恢复姿态时点击“回准备位”，确认完整路径清空；完成后仍保持，回工作台点击开始并重新录制。回位与补偿期间不会录制维护动作。紧急暂停会把活动集标为中断；解除锁存不会接着执行旧动作。

三路预览可随时关闭，采集与落盘继续。预览最多5Hz，不等于相机采集帧率；卡片显示的fps来自相机帧序号变化，原始采集默认30Hz。


## 任务卡与独立连接

悟演智能工作台使用白色界面，顶部依次为连接相机、连接机械臂、软件急停。先在任务卡创建“乐高分拣”等任务，填写明确采集目标，例如“将桌面乐高按颜色放入对应收纳盒”。已有任务可重复选择，不需要每次新建。

任务目录保存在本机 `data/tasks/tasks.json`，不提交Git。原始会话与每集manifest的 `task` 是英文指令字符串；`collection_task` 保存中文名称、中文说明、英文task和稳定UUID。LeRobot任务文本使用英文task，`provenance.json`保留两种字段。界面数据目录为 `<output根目录>/<任务UUID>/session_时间_随机后缀/`，中文名称不直接用作路径。原始CLI无界面仍使用配置任务，不强制使用本机任务库。

相机连接只启动三路相机，未连接机械臂也可检查画面。机械臂连接只启动四臂并保持；未选择
任务时进入不录制的遥操作；保持时可首次绑定任务并原子迁移会话，之后按正式采集上下文运行。数据采集、HIL和推理
开始前要求三路相机就绪；纯遥操作不要求相机。断开机械臂后相机继续预览；断开相机需先暂停、
结束录制与维护。首次绑定后正在机械臂会话时不能更换任务，断开并完成原始保存后可选下一任务，无需转换，
防止混写。任务目录损坏会显示错误并拒绝覆盖。

预览仍由独立进程最多5Hz编码，浏览器最多4Hz请求最新JPEG；原始采集与记录不等待浏览器。任务目录读写发生在交互线程，不进入逐帧控制和视频落盘路径。


### 视觉优先的采集布局（2026-09-09）

任务改为紧凑栏，任务身份与目录说明放入“详情”；四模式使用同一切换条。视觉区以全局主画面和两路腕部画面为主体，可点“放大视觉区/恢复布局”。开始、暂停、录制、弃集与结果标记集中在右侧；仅HIL模式显示介入/交还按钮。急停在连接按钮旁保持大尺寸，未连接时禁用。日志默认折叠。布局调整不改变预览编码分辨率、频率或记录链路。

参考具身采集与回读流程：[LeRobot可视化](https://huggingface.co/docs/lerobot/using_dataset_tools)、[phospho采集指引](https://docs.phospho.ai/learn/improve-robotics-ai-model)。本界面布局是针对本工作站的设计，不声明与这些产品完全相同或达到行业认证。


### 中文显示名与英文 task

创建/编辑任务时分别填写：`name`（中文名称，供采集员识别）、`instruction`（中文操作说明）与必填 `task`（英文模型/数据集指令，最长300字符）。例如中文名“乐高分拣”，task为 `Sort the LEGO bricks by color into the matching bins.`。模型prompt、`station.task_name`、原始数据的`task`及LeRobot任务文本均使用英文task；中文字段保存在`collection_task`供追溯。

旧任务可正常加载，须在“详情→编辑任务”补填英文task才能连接机械臂开始新会话；保留原UUID。正在运行或保存的会话不允许修改任务。编辑只影响后续会话，历史文件不迁移、不改写。无界面CLI仍使用配置文件任务名。

维护页面使用蓝灰配色，准备位与重力补偿各一组操作，四个按钮最小高度78px，显示动作说明。大按钮沿用原有保持/录制互锁和操作确认，不改变电机控制路径。


### 字体与全屏工作台

统一采用深蓝主色、深灰正文：页面标题26px、区块标题16px、正文/按钮主要14px、辅助信息最低12px；状态和核心指标加粗。顶部“全屏”可进入/退出浏览器全屏，不支持时提示最大化窗口。主区域不限制固定最大宽度，1920×1080与2560×1440已验证无横向溢出；大屏设备页将维护和关节调试并排显示。

桌面端工作台固定为单屏布局，不依赖页面纵向滚动：遥操作仅显示视觉、运动控制和相关设备健康；
正式采集把视觉、采集控制和健康信息横向排列，会话指标压缩为状态条；设备页把五步初始化向导
压成顶部步骤条，下方同时显示四臂、维护和点动。窄屏移动端保留自然滚动，避免缩小触控目标。


### 系统应用风格（2026-09-09后续）

界面样式已整体整理：浅灰背景、白色分组、少量蓝色强调、系统字体栈、统一圆角和字重；苹果设备优先系统字体，Linux使用可用中文字体回退。采集/设备双页、全屏、任务字段与所有控制接口不变。字体与布局以当前样式表为准，不再叠加历史配色补丁。
